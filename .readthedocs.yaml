version: 2

build:
  os: ubuntu-20.04
  apt_packages:
    - julia
  tools:
    python: "2.7"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt

formats: all